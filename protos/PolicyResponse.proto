// PolicyResponse.proto

syntax = "proto3";

package dgre;

import "RiskState.proto";

// Response containing recommended actions and computed risk
message PolicyResponse {
    repeated string recommended_agent_ids = 1; // Agents selected for intervention
    repeated RiskState updated_risk_states = 2; // Risk states after applying policy
    string message = 3; // Optional human-readable info
}
